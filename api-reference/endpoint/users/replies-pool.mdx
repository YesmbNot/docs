---
title: "Get Replies"
description: "GET paginated replies from a Twitter user via ApiTwitter pool. Retrieve reply tweets with cursor pagination. No cookies needed."
openapi: "GET /twitter/user/{userName}/replies"
---

<Info>
This endpoint uses the server-side cookie/proxy pool. You only need your API key. Use `count` and `cursor` query parameters for pagination.
</Info>
