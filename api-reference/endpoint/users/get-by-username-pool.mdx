---
title: "Get User by Username (GET)"
openapi: "GET /twitter/user/{userName}"
---

<Info>
This GET endpoint uses the server-side cookie/proxy pool. You only need your API key — no cookie or proxy required. The equivalent POST endpoint is available if you want to use your own cookies and proxy.
</Info>
