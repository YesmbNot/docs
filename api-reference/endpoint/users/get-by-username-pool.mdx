---
title: "Get User by Username"
description: "GET Twitter user profile by username via ApiTwitter pool endpoint. Returns followers, bio, verified status. No cookies needed."
openapi: "GET /twitter/user/{userName}"
---

<Info>
This endpoint uses the server-side cookie/proxy pool. You only need your API key — no cookie or proxy required.
</Info>
