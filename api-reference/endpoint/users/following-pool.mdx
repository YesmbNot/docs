---
title: "Get Following"
description: "GET paginated list of accounts a Twitter user follows. ApiTwitter pool endpoint with cursor pagination. No cookies needed."
openapi: "GET /twitter/user/{userName}/following"
---

<Info>
This endpoint uses the server-side cookie/proxy pool. You only need your API key. Use `count` and `cursor` query parameters for pagination.
</Info>
